
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.sportsHistry.repository.TimeLineMapper">

  <select id="selectTimeLine" resultType="com.example.sportsHistry.model.TimeLineEvent">
      SELECT * FROM timeline_event
    WHERE country = #{country}
    <if test="startYear != null">
      AND year &gt;= #{startYear}
    </if>
    <if test="endYear != null">
      AND year &lt;= #{endYear}
    </if>
    ORDER BY year
  </select>
  
   <select id="findByCountry" resultType="com.example.sportsHistry.model.TimeLineEvent">
    SELECT * FROM timeline_event
    WHERE country = #{country}
    ORDER BY year
  </select>
  
     <select id="findByCountryAndSports" resultType="com.example.sportsHistry.model.TimeLineEvent">
        SELECT id, country, year, event, detail, sports,is_important AS important
        FROM timeline_event
        WHERE country = #{country} AND sports = #{sports}
        ORDER BY year
    </select>
    
    <select id="findBySports" resultType="com.example.sportsHistry.model.TimeLineEvent">
    	SELECT * FROM timeline_event
    	WHERE sports = #{sports}
    </select>
    


</mapper>
